#if 0
foreach c {d s g z q Q w c o u x X} {
  scan $c %c x
  set n($c) $x
}
#define IGNORED $arbitrary @tokens
#if 1
`more` \skipped
#endif
/* A directive-looking line inside a comment is not a directive:
#endif
*/
#else
static int from_header = 17;
#endif
