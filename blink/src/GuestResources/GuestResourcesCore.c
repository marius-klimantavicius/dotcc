/* Full-core wrapper: all upstream record declarations must observe the same
 * qualified host-binding preamble as the unchanged bound translation units. */
#include "host-bindings.h"
#include "GuestResources.c"
